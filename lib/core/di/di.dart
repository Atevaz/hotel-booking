import 'package:booking_hotel/business_logic/global_cubit/global_cubit.dart';
import 'package:get_it/get_it.dart';

final sl = GetIt.instance;

Future init() async {
  /// ui cubit
  // sl.registerFactory(() => GlobalCubit(sl()));
  // sl.registerFactory(() => GlobalCubit(sl()));
  // sl.registerFactory(() => GlobalCubit(sl()));
  // sl.registerFactory(() => GlobalCubit(sl()));
  // sl.registerFactory(() => GlobalCubit(sl()));

  /// repo
  // sl.registerSingleton(() => repo(sl()));
  // sl.registerSingleton(() => repo(sl()));
  // sl.registerSingleton(() => repo(sl()));

  /// external lib
}
